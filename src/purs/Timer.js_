/* global exports */
"use strict";

// module Timer

exports.timeout = function (ms) {
  return function (cb) {
    return function () {
      return setTimeout(cb, ms);
    };
  };
};

exports.clearTimeout = function (tid) {
  clearTimeout(tid);
  return {};
};
